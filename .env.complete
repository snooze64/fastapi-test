# RAG-Anything 簡素化API用環境変数設定
# env.exampleの全環境変数を簡素化APIで利用可能

### Server Configuration
HOST=0.0.0.0
PORT=8000
WEBUI_TITLE=Simple RAG-Anything API
WEBUI_DESCRIPTION=簡素化されたRAG-Anything API（env.example対応）
WORKERS=1
CORS_ORIGINS=*

### Login Configuration (基本認証 - 実装されていないがパラメータは読み取り可能)
# AUTH_ACCOUNTS=admin:admin123,user1:pass456
# TOKEN_SECRET=Your-Key-For-Simple-RAG-API
# TOKEN_EXPIRE_HOURS=48

### Directory Configuration
INPUT_DIR=./uploads
OUTPUT_DIR=./output
WORKING_DIR=./rag_storage
LOG_DIR=./logs

### RAGAnything Configuration (Multimodal Document Processing)
PARSE_METHOD=auto
PARSER=mineru
DISPLAY_CONTENT_STATS=true
ENABLE_IMAGE_PROCESSING=true
ENABLE_TABLE_PROCESSING=true
ENABLE_EQUATION_PROCESSING=true
MAX_CONCURRENT_FILES=1

### Context Extraction Configuration
CONTEXT_WINDOW=1
CONTEXT_MODE=page
MAX_CONTEXT_TOKENS=2000

### Logging Configuration
LOG_LEVEL=INFO
VERBOSE=false

### LLM Configuration
LLM_BINDING=openai
LLM_MODEL=gpt-4o-mini
LLM_BINDING_HOST=https://api.openai.com/v1
LLM_BINDING_API_KEY=
TEMPERATURE=0
MAX_TOKENS=32768
TIMEOUT=240

# OpenAI API Key (LLM_BINDING_API_KEYと同じ)
OPENAI_API_KEY=

### Embedding Configuration
EMBEDDING_BINDING=openai
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIM=3072
EMBEDDING_BINDING_HOST=https://api.openai.com/v1
EMBEDDING_BINDING_API_KEY=

### 参考：env.exampleの他の設定（簡素化APIでは未使用だが、将来拡張時に利用可能）
# SUMMARY_LANGUAGE=English
# ENABLE_LLM_CACHE=true
# ENABLE_LLM_CACHE_FOR_EXTRACT=true
# MAX_ASYNC=4
# CHUNK_SIZE=1200
# CHUNK_OVERLAP_SIZE=100
# COSINE_THRESHOLD=0.2
# TOP_K=60

### データベース設定（簡素化APIでは未使用、必要時に実装）
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=your_username
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=your_database