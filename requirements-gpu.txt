# RAG-Anything GPU Requirements
# Base requirements
huggingface_hub
lightrag-hku
mineru[core]
tqdm

# GPU-optimized packages (CUDA 11.1 compatible)
# torch==1.10.2+cu111 and torchvision==0.11.3+cu111 will be installed via Dockerfile
# Using older but stable versions compatible with CUDA 11.1

# CUDA-accelerated packages for better performance
nvidia-ml-py3
py3nvml

# Optional image processing (GPU accelerated when available)
pillow>=10.0.0
opencv-python-headless

# Additional ML packages that benefit from GPU (CUDA 11.1 compatible)
transformers>=4.12.0,<4.22.0
sentence-transformers>=2.1.0,<2.3.0
accelerate>=0.12.0,<0.15.0

# FastAPI and web dependencies
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
python-multipart>=0.0.5
aiofiles

# Monitoring and logging
psutil